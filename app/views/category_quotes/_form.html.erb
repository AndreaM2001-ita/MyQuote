<%= form_with(model: category_quote) do |form| %>
  <% if category_quote.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category_quote.errors.count, "error") %> prohibited this category_quote from being saved:</h2>

      <ul>
        <% category_quote.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :quote_id, style: "display: block" %>
    <%= form.text_field :quote_id %>
  </div>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
