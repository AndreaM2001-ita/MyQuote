<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red; margin-bottom: 1em;">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>
      <ul>
        <% category.errors.full_messages.each do |message| %>if quote_params[:category_quotes_attributes].present?
        category_quote = @quote.category_quotes.new(
          category_id: category_quote_attr[:category_id]
        )
        categories_quote.save
      end
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :catName, "Category Name", class: "form-label" %>
    <%= form.text_field :catName, placeholder: "Enter category name", class: "form-control" %>
  </div>
<hr>
  <div class="form-actions">
    <%= form.submit "Save Category", class: "btn btn-primary" %>
  </div>
<% end %>